// frontend/vue.config.js
module.exports = {
  devServer: {
    host: "0.0.0.0",
    port: process.env.VUE_PORT || 8080,
    allowedHosts: "all",
    client: {
      webSocketURL: "auto://0.0.0.0:0/ws"
    }
  }
};